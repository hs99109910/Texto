<?xml version="1.0" encoding="utf-8"?>
<!-- Static placeholder shown before TextoGlass.applyPanel() replaces this view's
     background with the themed frosted-glass drawable at runtime. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/texto_dark_grey" />
    <corners android:radius="1000dp" />
</shape>
