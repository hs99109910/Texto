<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_coordinator"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <org.fossify.commons.views.MyAppBarLayout
        android:id="@+id/main_appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:alpha="0.92"
        app:elevation="0dp">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/main_toolbar"
            android:layout_width="match_parent"
            android:layout_height="70dp"
            android:background="@android:color/transparent"
            app:popupTheme="@style/NovaPopupTheme">

            <!--
                The design's header, pinned to left-to-right on purpose: the wordmark sits in
                the top-left corner and the gear in the top-right, whichever way the rest of
                the app reads. Everything below this row still follows the locale.
            -->
            <LinearLayout
                android:id="@+id/nova_header_row"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center_vertical"
                android:layoutDirection="ltr"
                android:orientation="horizontal"
                android:paddingStart="22dp"
                android:paddingEnd="22dp">

                <!--
                    The wordmark is a Latin name, not a translated word, so it keeps its own
                    direction whichever locale is running.
                -->
                <TextView
                    android:id="@+id/nova_title"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:includeFontPadding="false"
                    android:layoutDirection="ltr"
                    android:letterSpacing="-0.02"
                    android:maxLines="1"
                    android:text="@string/app_launcher_name"
                    android:textAlignment="viewStart"
                    android:textColor="@android:color/white"
                    android:textDirection="ltr"
                    android:textSize="27sp" />

                <!--
                    The one header control. Everything the old overflow offered is reachable
                    from settings now: archived conversations under their own heading, and
                    "about" as the last card, so a second button had nothing left to open.
                -->
                <ImageView
                    android:id="@+id/nova_menu_btn"
                    android:layout_width="38dp"
                    android:layout_height="38dp"
                    android:layout_gravity="center_vertical"
                    android:background="@drawable/nova_header_tile_bg"
                    android:clickable="true"
                    android:contentDescription="@string/settings_title"
                    android:focusable="true"
                    android:padding="10dp"
                    android:src="@drawable/ic_ph_gear_six"
                    app:tint="@android:color/white" />

            </LinearLayout>

        </com.google.android.material.appbar.MaterialToolbar>

        <include
            android:id="@+id/selection_bar"
            layout="@layout/layout_selection_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </org.fossify.commons.views.MyAppBarLayout>

    <!-- MAIN CONTENT BELOW TOP BAR -->
    <RelativeLayout
        android:id="@+id/main_nested_scrollview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:scrollbars="none">

        <androidx.coordinatorlayout.widget.CoordinatorLayout
            android:id="@+id/main_coordinator_wrapper"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <RelativeLayout
                android:id="@+id/main_holder"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <com.google.android.material.progressindicator.LinearProgressIndicator
                    android:id="@+id/conversations_progress_bar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_alignParentTop="true"
                    android:indeterminate="true"
                    android:visibility="gone"
                    app:hideAnimationBehavior="outward"
                    app:showAnimationBehavior="inward"
                    app:showDelay="250"
                    tools:visibility="visible" />

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/filter_bar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_alignParentTop="true"
                    android:layout_marginHorizontal="12dp"
                    android:alpha="0.92"
                    android:elevation="10dp"
                    android:clipChildren="false"
                    android:clipToPadding="false"
                    android:overScrollMode="never"
                    android:paddingHorizontal="12dp"
                    android:paddingTop="10dp"
                    android:paddingBottom="4dp"
                    android:scrollbars="none" />

                <org.fossify.commons.views.MyTextView
                    android:id="@+id/no_conversations_placeholder"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_below="@+id/filter_bar"
                    android:layout_centerHorizontal="true"
                    android:layout_marginTop="@dimen/bigger_margin"
                    android:alpha="0.8"
                    android:gravity="center"
                    android:paddingStart="@dimen/activity_margin"
                    android:paddingEnd="@dimen/activity_margin"
                    android:text="@string/no_conversations_found"
                    android:textSize="@dimen/bigger_text_size"
                    android:textStyle="italic"
                    android:visibility="gone" />

                <org.fossify.commons.views.MyTextView
                    android:id="@+id/no_conversations_placeholder_2"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_below="@+id/no_conversations_placeholder"
                    android:layout_centerHorizontal="true"
                    android:background="@drawable/ripple_all_corners"
                    android:gravity="center"
                    android:padding="@dimen/activity_margin"
                    android:text="@string/start_conversation"
                    android:textSize="@dimen/bigger_text_size"
                    android:visibility="gone" />

                <org.fossify.commons.views.MyRecyclerView
                    android:id="@+id/conversations_list"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout_alignParentTop="true"
                    android:layoutAnimation="@anim/layout_animation"
                    android:overScrollMode="ifContentScrolls"
                    android:scrollbars="none"
                    android:paddingBottom="100dp"
                    android:paddingHorizontal="4dp"
                    android:clipChildren="false"
                    android:clipToPadding="false"
                    app:layoutManager="org.fossify.commons.views.MyLinearLayoutManager" />
            </RelativeLayout>
        </androidx.coordinatorlayout.widget.CoordinatorLayout>

        <!-- SEARCH OVERLAY -->
        <RelativeLayout
            android:id="@+id/search_holder"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:alpha="0"
            android:visibility="gone">

            <org.fossify.commons.views.MyTextView
                android:id="@+id/search_placeholder"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="@dimen/activity_margin"
                android:alpha="0.8"
                android:gravity="center"
                android:paddingStart="@dimen/activity_margin"
                android:paddingEnd="@dimen/activity_margin"
                android:text="@string/no_items_found"
                android:textSize="@dimen/bigger_text_size"
                android:textStyle="italic" />

            <org.fossify.commons.views.MyTextView
                android:id="@+id/search_placeholder_2"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@+id/search_placeholder"
                android:layout_centerHorizontal="true"
                android:alpha="0.8"
                android:gravity="center"
                android:paddingStart="@dimen/activity_margin"
                android:paddingTop="@dimen/medium_margin"
                android:paddingEnd="@dimen/activity_margin"
                android:paddingBottom="@dimen/medium_margin"
                android:text="@string/type_2_characters"
                android:textSize="@dimen/bigger_text_size"
                android:textStyle="italic" />

            <org.fossify.commons.views.MyRecyclerView
                android:id="@+id/search_results_list"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:scrollbars="none"
                app:layoutManager="org.fossify.commons.views.MyLinearLayoutManager" />

        </RelativeLayout>
    </RelativeLayout>

    <!--
        NAVIGATION / SEARCH BAR. The design's floating pill: three places you browse, in
        locale order, with the compose action kept out of it as its own disc. 5dp of padding
        and 3dp between tabs, matching the mockup.
    -->
    <LinearLayout
        android:id="@+id/nova_nav_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginHorizontal="16dp"
        android:layout_marginBottom="22dp"
        android:background="@drawable/nova_searchbar_bg"
        android:elevation="10dp"
        android:orientation="horizontal"
        android:padding="6dp"
        android:gravity="center_vertical">

        <!--
            COMPOSE. First child, so under the app's right-to-left layout it lands against
            the far right edge of the capsule.
        -->
        <LinearLayout
            android:id="@+id/nav_add_btn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical"
            android:paddingHorizontal="4dp"
            android:paddingVertical="10dp">
            <ImageView
                android:id="@+id/nav_add_icon"
                android:layout_width="23dp"
                android:layout_height="23dp"
                android:src="@drawable/ic_ph_plus"
                app:tint="@android:color/white" />
            <TextView
                android:id="@+id/nav_add_label"
                style="@style/NavBarLabel"
                android:text="@string/nav_new_message" />
        </LinearLayout>

        <!-- THREADS. The screen you are on. -->
        <LinearLayout
            android:id="@+id/nav_home_btn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="3dp"
            android:background="@drawable/nova_nav_tab_active"
            android:gravity="center"
            android:orientation="vertical"
            android:paddingHorizontal="4dp"
            android:paddingVertical="10dp">
            <ImageView
                android:id="@+id/nav_home_icon"
                android:layout_width="23dp"
                android:layout_height="23dp"
                android:src="@drawable/ic_ph_chat_teardrop_text"
                app:tint="@android:color/white" />
            <TextView
                android:id="@+id/nav_home_label"
                style="@style/NavBarLabel"
                android:text="@string/nav_conversations" />
        </LinearLayout>

        <!-- SEARCH BUTTON / INPUT CONTAINER -->
        <LinearLayout
            android:id="@+id/nav_search_container"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="3dp"
            android:orientation="horizontal"
            android:paddingHorizontal="4dp"
            android:paddingVertical="10dp"
            android:gravity="center">

            <!--
                Icon and label are stacked so the collapsed state matches the other two tabs.
                When the search field expands, MainActivity hides the label and this column
                collapses back to just the icon, leaving the row free for the input.
            -->
            <LinearLayout
                android:id="@+id/nav_search_stack"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="vertical">
                <ImageView
                    android:id="@+id/nova_search_icon"
                    android:layout_width="23dp"
                    android:layout_height="23dp"
                    android:src="@drawable/ic_ph_magnifying_glass"
                    app:tint="@android:color/white" />
                <TextView
                    android:id="@+id/nav_search_label"
                    style="@style/NavBarLabel"
                    android:text="@string/search" />
            </LinearLayout>

            <EditText
                android:id="@+id/nova_search_input"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:textAlignment="viewStart"
                android:textDirection="locale"
                android:paddingStart="8dp"
                android:paddingEnd="12dp"
                android:hint="@string/search"
                android:textColor="@android:color/white"
                android:textColorHint="#888888"
                android:background="@android:color/transparent"
                android:imeOptions="actionSearch"
                android:inputType="text"
                android:visibility="gone" />
        </LinearLayout>

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
