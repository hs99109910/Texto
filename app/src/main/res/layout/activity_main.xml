<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_coordinator"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <com.texto.sms.views.TextoAppBarLayout
        android:id="@+id/main_appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        app:elevation="0dp">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/main_toolbar"
            android:layout_width="match_parent"
            android:layout_height="58dp"
            android:background="@android:color/transparent"
            app:popupTheme="@style/TextoPopupTheme">

            <!--
                The design's header, pinned to left-to-right on purpose: the wordmark sits in
                the top-left corner and the gear in the top-right, whichever way the rest of
                the app reads. Everything below this row still follows the locale.
            -->
            <LinearLayout
                android:id="@+id/texto_header_row"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center_vertical"
                android:layoutDirection="ltr"
                android:orientation="horizontal"
                android:paddingStart="22dp"
                android:paddingEnd="22dp">

                <!--
                    The wordmark, taken as the design's own logo image (its neon-outline
                    "Texto" mark) rather than redrawn as styled text. It keeps its own
                    direction whichever locale is running, same as the text it replaces did.
                -->
                <ImageView
                    android:id="@+id/texto_title"
                    android:layout_width="0dp"
                    android:layout_height="37dp"
                    android:layout_weight="1"
                    android:layoutDirection="ltr"
                    android:adjustViewBounds="true"
                    android:contentDescription="@string/app_launcher_name"
                    android:scaleType="fitStart"
                    android:src="@drawable/img_texto_wordmark" />

                <!--
                    The one header control. Everything the old overflow offered is reachable
                    from settings now: archived conversations under their own heading, and
                    "about" as the last card, so a second button had nothing left to open.
                -->
                <ImageView
                    android:id="@+id/texto_menu_btn"
                    android:layout_width="38dp"
                    android:layout_height="38dp"
                    android:layout_gravity="center_vertical"
                    android:background="@drawable/texto_header_tile_bg"
                    android:clickable="true"
                    android:contentDescription="@string/settings_title"
                    android:focusable="true"
                    android:padding="10dp"
                    android:src="@drawable/ic_ph_gear_six"
                    app:tint="@android:color/white" />

            </LinearLayout>

        </com.google.android.material.appbar.MaterialToolbar>

        <include
            android:id="@+id/selection_bar"
            layout="@layout/layout_selection_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </com.texto.sms.views.TextoAppBarLayout>

    <!-- MAIN CONTENT BELOW TOP BAR -->
    <RelativeLayout
        android:id="@+id/main_nested_scrollview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:scrollbars="none">

        <androidx.coordinatorlayout.widget.CoordinatorLayout
            android:id="@+id/main_coordinator_wrapper"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <RelativeLayout
                android:id="@+id/main_holder"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <com.google.android.material.progressindicator.LinearProgressIndicator
                    android:id="@+id/conversations_progress_bar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_alignParentTop="true"
                    android:indeterminate="true"
                    android:visibility="gone"
                    app:hideAnimationBehavior="outward"
                    app:showAnimationBehavior="inward"
                    app:showDelay="250"
                    tools:visibility="visible" />

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/filter_bar"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_alignParentTop="true"
                    android:layout_centerHorizontal="true"
                    android:layout_marginHorizontal="12dp"
                    android:elevation="10dp"
                    android:clipChildren="false"
                    android:clipToPadding="false"
                    android:overScrollMode="never"
                    android:paddingHorizontal="12dp"
                    android:paddingTop="4dp"
                    android:paddingBottom="3dp"
                    android:scrollbars="none" />

                <org.fossify.commons.views.MyTextView
                    android:id="@+id/no_conversations_placeholder"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_below="@+id/filter_bar"
                    android:layout_centerHorizontal="true"
                    android:layout_marginTop="@dimen/bigger_margin"
                    android:alpha="0.8"
                    android:gravity="center"
                    android:paddingStart="@dimen/activity_margin"
                    android:paddingEnd="@dimen/activity_margin"
                    android:text="@string/no_conversations_found"
                    android:textSize="@dimen/bigger_text_size"
                    android:textStyle="italic"
                    android:visibility="gone" />

                <org.fossify.commons.views.MyTextView
                    android:id="@+id/no_conversations_placeholder_2"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_below="@+id/no_conversations_placeholder"
                    android:layout_centerHorizontal="true"
                    android:background="@drawable/ripple_all_corners"
                    android:gravity="center"
                    android:padding="@dimen/activity_margin"
                    android:text="@string/start_conversation"
                    android:textSize="@dimen/bigger_text_size"
                    android:visibility="gone" />

                <com.texto.sms.views.TextoRecyclerView
                    android:id="@+id/conversations_list"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout_alignParentTop="true"
                    android:layoutAnimation="@anim/layout_animation"
                    android:overScrollMode="ifContentScrolls"
                    android:scrollbars="none"
                    android:paddingBottom="100dp"
                    android:paddingHorizontal="4dp"
                    android:clipChildren="false"
                    android:clipToPadding="false"
                    app:layoutManager="com.texto.sms.views.TextoLinearLayoutManager" />
            </RelativeLayout>
        </androidx.coordinatorlayout.widget.CoordinatorLayout>

        <!-- SEARCH OVERLAY -->
        <RelativeLayout
            android:id="@+id/search_holder"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:alpha="0"
            android:visibility="gone">

            <!--
                The search panel rides at the TOP of the screen rather than taking over the
                bottom capsule, so the nav pill stays reachable while you search. It carries
                the field, the same filter chips the home screen uses, a Jalali date range,
                and the result count: all four combine.
            -->
            <LinearLayout
                android:id="@+id/texto_search_panel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_alignParentTop="true"
                android:orientation="vertical"
                android:paddingHorizontal="12dp"
                android:paddingBottom="6dp">

                <LinearLayout
                    android:id="@+id/texto_search_capsule"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:orientation="horizontal"
                    android:paddingHorizontal="16dp"
                    android:paddingVertical="6dp">

                    <ImageView
                        android:id="@+id/texto_search_panel_icon"
                        android:layout_width="18dp"
                        android:layout_height="18dp"
                        android:importantForAccessibility="no"
                        android:src="@drawable/ic_ph_magnifying_glass" />

                    <EditText
                        android:id="@+id/texto_search_input"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:background="@android:color/transparent"
                        android:hint="@string/search_hint"
                        android:imeOptions="actionSearch"
                        android:inputType="text"
                        android:paddingHorizontal="12dp"
                        android:paddingVertical="10dp"
                        android:textAlignment="viewStart"
                        android:textColor="@android:color/white"
                        android:textColorHint="#888888"
                        android:textDirection="locale" />

                    <ImageView
                        android:id="@+id/texto_search_clear"
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:clickable="true"
                        android:contentDescription="@string/clear_search"
                        android:focusable="true"
                        android:src="@drawable/ic_ph_x" />

                </LinearLayout>

                <!--
                    Two rows, not one. Date and audience are different questions, and sharing
                    a line meant the date control ate the width the filter chips needed and
                    scrolled them half out of reach. Each now gets the full width, in the
                    order the panel reads: what to find, then when, then who.
                -->
                <HorizontalScrollView
                    android:id="@+id/search_date_bar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="10dp"
                    android:clipChildren="false"
                    android:clipToPadding="false"
                    android:overScrollMode="never"
                    android:scrollbars="none">

                    <LinearLayout
                        android:id="@+id/search_date_chips"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:clipChildren="false"
                        android:gravity="center_vertical"
                        android:orientation="horizontal"
                        android:paddingHorizontal="4dp" />

                </HorizontalScrollView>

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/search_filter_bar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:clipChildren="false"
                    android:clipToPadding="false"
                    android:overScrollMode="never"
                    android:paddingHorizontal="4dp"
                    android:scrollbars="none" />

                <TextView
                    android:id="@+id/search_result_count"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:paddingHorizontal="8dp"
                    android:textAlignment="viewStart"
                    tools:text="۱۲ نتیجه" />

            </LinearLayout>

            <org.fossify.commons.views.MyTextView
                android:id="@+id/search_placeholder"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@+id/texto_search_panel"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="@dimen/activity_margin"
                android:alpha="0.8"
                android:gravity="center"
                android:paddingStart="@dimen/activity_margin"
                android:paddingEnd="@dimen/activity_margin"
                android:text="@string/no_items_found"
                android:textSize="@dimen/bigger_text_size"
                android:textStyle="italic" />

            <org.fossify.commons.views.MyTextView
                android:id="@+id/search_placeholder_2"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@+id/search_placeholder"
                android:layout_centerHorizontal="true"
                android:alpha="0.8"
                android:gravity="center"
                android:paddingStart="@dimen/activity_margin"
                android:paddingTop="@dimen/medium_margin"
                android:paddingEnd="@dimen/activity_margin"
                android:paddingBottom="@dimen/medium_margin"
                android:text="@string/type_2_characters"
                android:textSize="@dimen/bigger_text_size"
                android:textStyle="italic" />

            <com.texto.sms.views.TextoRecyclerView
                android:id="@+id/search_results_list"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_below="@+id/texto_search_panel"
                android:clipChildren="false"
                android:clipToPadding="false"
                android:paddingBottom="120dp"
                android:paddingHorizontal="4dp"
                android:scrollbars="none"
                app:layoutManager="com.texto.sms.views.TextoLinearLayoutManager" />

        </RelativeLayout>
    </RelativeLayout>

    <!--
        NAVIGATION / SEARCH BAR. The design's floating pill: three places you browse, in
        locale order, with the compose action kept out of it as its own disc. 5dp of padding
        and 3dp between tabs, matching the mockup.
    -->
    <FrameLayout
        android:id="@+id/texto_nav_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginHorizontal="16dp"
        android:layout_marginBottom="22dp"
        android:background="@drawable/texto_searchbar_bg"
        android:elevation="10dp"
        android:padding="6dp">

        <!--
            The halo behind the current tab. A view of its own rather than a background on
            the tab, so it can slide from one to the next instead of blinking across: its
            width and height are set from the tab it is on and only translationX animates.
        -->
        <View
            android:id="@+id/nav_halo"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_gravity="top" />

        <LinearLayout
            android:id="@+id/texto_nav_row"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

        <!--
            COMPOSE. First child, so under the app's right-to-left layout it lands against
            the far right edge of the capsule.
        -->
        <LinearLayout
            android:id="@+id/nav_add_btn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical"
            android:paddingHorizontal="4dp"
            android:paddingVertical="10dp">
            <ImageView
                android:id="@+id/nav_add_icon"
                android:layout_width="23dp"
                android:layout_height="23dp"
                android:src="@drawable/ic_ph_plus"
                app:tint="@android:color/white" />
            <TextView
                android:id="@+id/nav_add_label"
                style="@style/NavBarLabel"
                android:text="@string/nav_new_message" />
        </LinearLayout>

        <!-- THREADS. The screen you are on. -->
        <LinearLayout
            android:id="@+id/nav_home_btn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="3dp"
            android:background="@drawable/texto_nav_tab_active"
            android:gravity="center"
            android:orientation="vertical"
            android:paddingHorizontal="4dp"
            android:paddingVertical="10dp">
            <ImageView
                android:id="@+id/nav_home_icon"
                android:layout_width="23dp"
                android:layout_height="23dp"
                android:src="@drawable/ic_ph_chat_teardrop_text"
                app:tint="@android:color/white" />
            <TextView
                android:id="@+id/nav_home_label"
                style="@style/NavBarLabel"
                android:text="@string/nav_conversations" />
        </LinearLayout>

        <!-- SEARCH BUTTON / INPUT CONTAINER -->
        <LinearLayout
            android:id="@+id/nav_search_container"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="3dp"
            android:orientation="horizontal"
            android:paddingHorizontal="4dp"
            android:paddingVertical="10dp"
            android:gravity="center">

            <!--
                A plain tab now. The field it used to expand into lives in the top search
                panel instead, so this capsule keeps its three tabs while you search.
            -->
            <LinearLayout
                android:id="@+id/nav_search_stack"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="vertical">
                <ImageView
                    android:id="@+id/texto_search_icon"
                    android:layout_width="23dp"
                    android:layout_height="23dp"
                    android:src="@drawable/ic_ph_magnifying_glass"
                    app:tint="@android:color/white" />
                <TextView
                    android:id="@+id/nav_search_label"
                    style="@style/NavBarLabel"
                    android:text="@string/search" />
            </LinearLayout>
        </LinearLayout>

        </LinearLayout>

    </FrameLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
