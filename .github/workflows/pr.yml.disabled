name: PR

on:
    pull_request:
        branches: [main]

permissions:
  contents: read
  pull-requests: read

jobs:
    call-pr-workflow:
        uses: NovaOrg/.github/.github/workflows/pr.yml@main
